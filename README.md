# Auditorium-Ticket-Reservation
Code that receives a file for auditorium seating and then allows users to select seats. It also creates a text file to store the earnings of the auditorium and other information.
